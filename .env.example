# Security Keys (GENERATE THESE - DO NOT USE EXAMPLES)
# Generate with: python -c "from cryptography.fernet import Fernet; import secrets; print(f'SECRET_KEY={secrets.token_hex(32)}'); print(f'ENCRYPTION_KEY={Fernet.generate_key().decode()}'); print(f'EMAIL_BLIND_INDEX_SALT={secrets.token_hex(16)}')"
SECRET_KEY=your-secret-key-here
ENCRYPTION_KEY=your-fernet-key-here
EMAIL_BLIND_INDEX_SALT=your-salt-here

# First Run Admin User (only used on initial setup)
FIRST_RUN_ADMIN_USER=admin
FIRST_RUN_ADMIN_PASSWORD=change-this-password

# Database
DATABASE_URL=sqlite:////app/data/members.db

# CORS
FRONTEND_URL=http://localhost:5173

# JWT Settings
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=480
